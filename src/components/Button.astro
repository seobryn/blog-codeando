---
interface Props {
  type?: "submit" | "reset" | "button";
}

const { type } = Astro.props;
---

<button
  type={type}
  class="h-12 bg-slate-500 text-white px-4 self-end rounded-md active:bg-slate-600"
  ><slot /></button
>
